pre
  code.lang-javascript
    | function* watchers() {
    .fragment
      |   // Every time a SAVE is seen begin
      |   // an instance of 'save' Saga
      |   yield fork(takeEvery("SAVE", save));
      |
    .fragment
      |   // Every time a REFRESH is seen begin
      |   // an instance of the 'refresh' Saga
      |   // Cancel any instances already running.
      |   yield fork(takeLatest("REFRESH", refresh));
    | }
    |
    | // Sagas
    | function* save(saveAction) {
    |   // Logic here
    | }
    | function* refresh(refreshAction) {
    |   // Logic here
    | }
